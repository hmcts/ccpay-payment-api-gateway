#!groovy
@Library("Infrastructure@2.4.3") _

properties([
		parameters([
			choice(name: 'ENVIRONMENT', choices: 'saat\nsprod\nsandbox', description: 'Environment where code should be build and deployed')
		])
])

withInfrastructurePipeline('paymentsgw', params.ENVIRONMENT, 'sandbox')